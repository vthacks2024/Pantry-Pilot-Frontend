import SwiftUI
import UIKit

struct DietaryRestrictions: View {
    @State private var storedImage: UIImage? = nil
    
    var body: some View {
        VStack {
            Text("This is the diet page")
        }
    }
}
